{
  "name": "Estructuras de Datos - Python",
  "image": "mcr.microsoft.com/devcontainers/python:3.12",
  "postCreateCommand": "pip install -r requirements.txt && git config merge.ours.driver true && git submodule update --init && git remote add upstream https://github.com/arleyfernandotorresgalindo/ED_2026_02_v2.git || true",
  "remoteEnv": {
    "PYTHONPATH": "${containerWorkspaceFolder}"
  },
  "customizations": {
    "vscode": {
      "extensions": [
        "ms-toolsai.jupyter",
        "ms-python.python"
      ]
    }
  }
}
